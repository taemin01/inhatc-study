#include <stdio.h>

int count;
char element;
float fvalue;

int main() {
    int i;
    printf("전역변수 count : %d\n", count);
    printf("전역변수 element : %c\n", element);
    printf("전역변수 fvalue : %f\n", fvalue);
    printf("%d", i);

    return 0;
}