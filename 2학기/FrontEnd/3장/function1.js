var kor = 80, com = 96, sum = 0, average = 0;

function tot(){ // tot() 함수 정의
  sum = kor + com;
  alert(sum);
}

function avg(){ // avg() 함수 정의
  average = sum / 2;
  alert(average);
}

tot(); // tot() 함수 호출
avg(); // avg() 함수 호출
