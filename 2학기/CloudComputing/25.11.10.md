- DBload가 기준을 넘지 않는지만 확인하면 된다
- 172.31.17.171/32는 이 ip만 허용
- systemctl이 최초 실행때는 사용해야 하지만 권한이 너무 강력해서 service 권장
- sudo service nginx status 등으로 sudo systemctl status nginx
- 서버 액세스 로깅 -> 로그 저장 대상 버킷은 따로 만들어서 해야함 대상을 해당 버킷으로 하면 무한 루프 로그가 됨
- s3 정적 웹사이트 자바스크립트 동작하기 때문에 백엔드 api 콜도 가능